const x = 5;
const y = 10;
const z = 11;

if (x !== y) {
    console.log('these are not equal');
}

if (x === y && y === z && isTrue) {
    console.log('these are all equal');
}

if (isTrue || x === z) {
    console.log('this is an or statement');
}

if (isTrue || (x === z && y === z)) {
    console.log('this is an or statement');
}